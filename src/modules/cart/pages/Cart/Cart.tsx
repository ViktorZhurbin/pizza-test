import { Layout } from '@/components/Layout';

import { Cart } from '../../components/Cart';

export const CartPage: React.FC = () => {
    return (
        <Layout>
            <Cart />
        </Layout>
    );
};
